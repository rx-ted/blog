package asia.rxted.blog.modules.verification.config;

public enum VerificationEnum {
    /**
     * 登录
     * 注册
     * 找回用户
     * 修改密码
     * 支付钱包密码
     * 绑定手机号
     */
    LOGIN,
    REGISTER,
    FIND_USER,
    UPDATE_PASSWORD,
    WALLET_PASSWORD,
    BIND_MOBILE;
}
