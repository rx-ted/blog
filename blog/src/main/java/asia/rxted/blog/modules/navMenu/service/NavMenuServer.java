package asia.rxted.blog.modules.navMenu.service;

import com.baomidou.mybatisplus.extension.service.IService;

import asia.rxted.blog.modules.navMenu.dto.NavMenu;

public interface NavMenuServer extends IService<NavMenu> {
    // 如果有不同的函数，可以放在这个
}
